[project]
name = "coati"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "grimp>=3.5",
 "networkx>=3.4.2",
 "rich>=13.9.4",
]

[dependency-groups]
dev = [
    "coverage>=7.6.10",
    "pytest>=8.3.4",
]
